
top_pane=$(tmux list-panes -F '#{pane_index} #{pane_top}' | sort -k2n | head -n1 | awk '{print $1}')

find . \
  -type d \( -name node_modules -o -name .git \) -prune \
  -o -type f -print | \
  fzf --bind "enter:execute(tmux select-pane -U && tmux send-keys -t $top_pane  C-r '{}' C-m)+abort"

